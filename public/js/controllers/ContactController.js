(function () {
    angular.module(QuizUp).controller('ContactController', ['$scope', '$rootScope', '$authenticationService', '$state', function ($scope, $rootScope, $authenticationService, $state) {
        let contact = this;

    }]);
})();